// import React from 'react';
// import { render } from '@testing-library/react';
// import Header from './Blogs'; // Import your Header component

// describe('Header Component', () => {
//   it('renders the header text correctly', () => {
//     const { getByText } = render(
//       <Header />
//     );

//     // Assert that the text "Relative POSTS" is present in the rendered component
//     const headerElement = getByText('Relative POSTS');
//     expect(headerElement).toBeInTheDocument();

//     // Optional: You can also assert on the className if needed
//     expect(headerElement).toHaveClass('font-bold text-2xl pb-6 uppercase');
//   });
// });
